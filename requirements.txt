# Runtime dependencies. Kept in sync with [project.dependencies] in
# pyproject.toml; the Dockerfile installs from this file.
flask>=3.1.1
click>=8.1.3
gunicorn>=23.0.0
python-dotenv>=1.0.0
werkzeug>=3.1.3
